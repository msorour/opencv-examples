cmake_minimum_required(VERSION 2.8)
project( manual_monitoring )
find_package( OpenCV REQUIRED )
add_executable( rate_prediction rate_prediction.cpp )
target_link_libraries( rate_prediction -llept -lncurses -ltesseract tesseract ${OpenCV_LIBS} ${Tesseract_LIBRARIES} ${LEPTONICA_LIBRARIES} )
